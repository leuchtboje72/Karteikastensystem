{% extends "base.html.twig" %}

{% block body %}
    {{ parent() }}

    <div class="panel">
        <div class="panel-heading">
            {{ user.username }}
        </div>
        <div class="panel-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}